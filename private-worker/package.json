{
  "name": "mehz-private-index",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": { "jose": "6.2.10" },
  "devDependencies": { "wrangler": "4.129.0" }
}
